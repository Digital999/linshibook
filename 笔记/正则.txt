impoet re

对象名=re.下列方法名（r'表达式'，‘原字符串’，flag）
	
	flag为修饰说明

			：如re.I 不区分大小写
			：re.M   多行匹配

match
	匹配字符串中的开头，头匹配失败则返回None


search

	在整个字符串中查找并匹配


findall
		
	前者都是返回一个对象，这里返回的是一个列表


complie

	用于为编写正则表达式，供上述表达式使用



sub
	用于替换匹配的字符
	对象名=re.sub(r'模式'，‘替换的内容（可以是函数）’,'原字符串',flag)