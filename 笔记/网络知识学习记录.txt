消息队列
消息是两台计算机传输的数据单位
消息队列是消息进行传输时消息的容器，消息队列管理器在传输过程中当中间人，若接收者不可用，则消息会暂时保存在队列中。知道传递成功。
消息队列就像一个链表。消息就想记录，有写权限的进程可以添加消息，有读权限的可以取走消息。
消息队列的类型：POSIX消息队列以及系统V消息队列。
消息队列就像链表里的队，每个队都有头，头部存储了两台计算机的信息，以及队列的信息，甚至记录了对鞋消息队列进程的id。
消息队列的主要特点是异步处理。主要目的是减少请求时间和解耦，削峰。
消息队列的缺点：系统可用性降低，复杂性增加。
如何保持消息队列的高可用性：